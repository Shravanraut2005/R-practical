#display first 15 rows of mt car data set
data("mtcars")
head(mtcars,15)

#find maximum and minimum value of sepal.length from iris
max(iris$Sepal.Length)

#calculate the mean of variable mpg in mtcars
mean(mtcars$mpg)

#display structure of airqulality Data Set
str(airquality)

#check whether number 100 is grater than 50 in R
100>50
